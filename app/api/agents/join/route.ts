// Backward-compatible alias. One implementation keeps registration, rate
// limits, validation, and claim behavior consistent across both public paths.
export { POST, dynamic } from '../register/route';
